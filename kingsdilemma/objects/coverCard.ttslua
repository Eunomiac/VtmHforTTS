local function getDilemmaDeck()
	-- searches beneath itself for a deck of cards, returns nil if none found
end

function DrawDilemma(params) -- = {player = Player who clicked the button}
	local deck = getDilemmaDeck()
	if deck == nil then return end
	-- draws a card from the BOTTOM of the deck and deals it to the current Leader
	-- dims spotlights except on Leader as they read the card
end